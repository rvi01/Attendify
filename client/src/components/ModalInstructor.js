import React from 'react';

const ModalInstructor = ({ onClose }) => {
  return (
    <div>
      ModalInstructor
      <button onClick={onClose}> CLICK</button>
    </div>
  );
};

export default ModalInstructor;
